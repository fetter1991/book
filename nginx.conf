user root;
pid logs/nginx.pid;
worker_processes  4;
worker_rlimit_nofile 65535;
events {
    worker_connections  65535;
}


# Settings that affect all server blocks
http {
	
    include php_processes.conf;
    include       mime.types;
    default_type  application/octet-stream;
    server_tokens off;
    client_max_body_size 1000m;
    sendfile on;
    keepalive_timeout  65;

    # http server
    server {
        listen       81;
        root   E:/spm;
        index index.php;
	
        add_header Processor "__CONTAINER_HOST_NAME__";
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept";
        add_header Access-Control-Allow-Methods GET,POST,OPTIONS;

        #location / {
        #    try_files $uri $uri/ /index.php?$query_string;
        #}

        location / {
            
        }

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        location ~ \.php$ {
            fastcgi_pass   php_processes;
            fastcgi_index  index.php;
            fastcgi_read_timeout 150;
            fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include        fastcgi_params;
        }

        # deny access to .htaccess files, if Apache's document root
        location ~ /\.ht {
            deny  all;
        }
    }
    #http server END

    # http server2
    server {
        listen       80;
        root   E:/admin;
        index index.php;

        add_header Processor "__CONTAINER_HOST_NAME__";
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept";

        #location / {
        #    try_files $uri $uri/ /index.php?$query_string;
        #}

        location / {
            if (!-e $request_filename) {
                rewrite  ^(.*)$  /index.php?s=$1  last;
            }
        }

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        location ~ \.php$ {
            fastcgi_pass   php_processes;
            fastcgi_index  index.php;
            fastcgi_read_timeout 150;
            fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include        fastcgi_params;
        }

        # deny access to .htaccess files, if Apache's document root
        location ~ /\.ht {
            deny  all;
        }
    }
    #http server END

    # http server3
    server {
        listen       82;
        root   H:/fetter1991/book1;
        index index.php;

        add_header Processor "__CONTAINER_HOST_NAME__";
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept";

        #location / {
        #    try_files $uri $uri/ /index.php?$query_string;
        #}

        location / {
            if (!-e $request_filename) {
                rewrite  ^(.*)$  /index.php?s=$1  last;
            }
        }

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        location ~ \.php$ {
            fastcgi_pass   php_processes;
            fastcgi_index  index.php;
            fastcgi_read_timeout 150;
            fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include        fastcgi_params;
        }

        # deny access to .htaccess files, if Apache's document root
        location ~ /\.ht {
            deny  all;
        }
    }
    #http server END

    # http server4（Hard disk）
    server {
        listen       83;
        root   H:/fetter1991/admin;
        index index.php;

        add_header Processor "__CONTAINER_HOST_NAME__";
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept";

        #location / {
        #    try_files $uri $uri/ /index.php?$query_string;
        #}

        location / {
            if (!-e $request_filename) {
                rewrite  ^(.*)$  /index.php?s=$1  last;
            }
        }

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        location ~ \.php$ {
            fastcgi_pass   php_processes;
            fastcgi_index  index.php;
            fastcgi_read_timeout 150;
            fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include        fastcgi_params;
        }

        # deny access to .htaccess files, if Apache's document root
        location ~ /\.ht {
            deny  all;
        }
    }
    #http server END

    # http server5（Hard disk）
    server {
        listen       84;
        root   H:/fetter1991/icemoun;
        index index.php;

        add_header Processor "__CONTAINER_HOST_NAME__";
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept";

        #location / {
        #    try_files $uri $uri/ /index.php?$query_string;
        #}

        location / {
            if (!-e $request_filename) {
                rewrite  ^(.*)$  /index.php?s=$1  last;
            }
        }

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        location ~ \.php$ {
            fastcgi_pass   php_processes;
            fastcgi_index  index.php;
            fastcgi_read_timeout 150;
            fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include        fastcgi_params;
        }

        # deny access to .htaccess files, if Apache's document root
        location ~ /\.ht {
            deny  all;
        }
    }
    #http server END

    # http server6（Hard disk）
    server {
        listen       85;
        root   E:/VueDraggable;
        index index.php;

        add_header Processor "__CONTAINER_HOST_NAME__";
        add_header Access-Control-Allow-Origin "*";
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept";

        #location / {
        #    try_files $uri $uri/ /index.php?$query_string;
        #}

        location / {
            if (!-e $request_filename) {
                rewrite  ^(.*)$  /index.php?s=$1  last;
            }
        }

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        location ~ \.php$ {
            fastcgi_pass   php_processes;
            fastcgi_index  index.php;
            fastcgi_read_timeout 150;
            fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include        fastcgi_params;
        }

        # deny access to .htaccess files, if Apache's document root
        location ~ /\.ht {
            deny  all;
        }
    }
    #http server END
}
